using System;
namespace BasicKnowledge
{
    /*
     * 判断语句
     * if 语句
     *      一个布尔表达式跟一个或 多个语句组成
     * if else 语句
     *      一个 if 后跟 一个 else
     * 嵌套 if 语句
     * switch 语句 
     * 嵌套 switch 语句    
     * 
     * ?:运算符
     *      Exp1 ? Exp2 : Exp3 ;
     *      ?表达式的值由 Exp1 决定，Exp1 为真则取 Exp2的值；Exp1为假，则取Exp3的值
     * 
     */
    public class Judge
    {
        public Judge()
        {
        }
    }
}
