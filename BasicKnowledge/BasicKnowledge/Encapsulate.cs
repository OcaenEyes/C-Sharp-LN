using System;
namespace BasicKnowledge
{
    /* 封装
     * C# 封装根据具体需要，设置使用者的访问权限，通过 访问修饰符 实现
     *  public  所有对象均可以访问
     *  private     对象本身可以在内部访问
     *  protected   只有该类对象，及其子类对象可以访问  
     *  internal    同一个程序集的对象可以访问
     *  protected internal      访问限于当前程序集 或 派生自包含类的 类型
     * 
     */

    public class Encapsulate
    {
        public Encapsulate()
        {
        }
    }
}
